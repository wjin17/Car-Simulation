import { createNascar } from "./nascar";
import { createDonut } from "./donut";
import { createMarioCircuit1 } from "./MushroomCup/marioCircuit1";

const Default = {
  createDonut,
  createNascar,
};

const Mario = {
  createMarioCircuit1,
};

export default {
  Default,
  Mario,
};
